{
  "name": "chat-clipper",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "postbuild": "cp manifest.json dist/"
  },
  "dependencies": {
    "dompurify": "^3.0.6",
    "preact": "^10.16.0"
  },
  "devDependencies": {
    "@babel/core": "7.x",
    "@preact/preset-vite": "^2.5.0",
    "vite": "^4.4.5"
  }
}
